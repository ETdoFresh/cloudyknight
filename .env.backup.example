# MinIO Backup Configuration
# Copy this file to .env.backup and fill in your credentials

# Local MinIO Configuration (usually default values)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Remote MinIO Configuration (REQUIRED)
# Your remote MinIO server at https://minio.etdofresh.com
REMOTE_MINIO_ENDPOINT=https://minio.etdofresh.com
REMOTE_MINIO_USER=your-remote-access-key
REMOTE_MINIO_PASSWORD=your-remote-secret-key

# Backup Configuration
BACKUP_MODE=mirror              # Options: mirror, sync, incremental
BACKUP_INTERVAL=3600            # Backup interval in seconds (default: 1 hour)
INITIAL_DELAY=60                # Delay before first backup in seconds

# Optional: Domain configuration (if different from default)
DOMAIN=etdofresh-dev.duckdns.org